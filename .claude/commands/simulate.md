Run the strategy simulation and report outcome distributions.

Steps:
1. Run `make simulate` from the project root
2. Report the output clearly, noting which strategy performs better and any balance concerns
